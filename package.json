{
  "name": "lerna-test",
  "version": "0.0.1",
  "main": "index.ts",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "./scripts/clean.sh all",
    "lint": "yarn workspace @ws/simple-react-app lint",
    "lintStaged": "lint-staged",
    "build": "yarn workspace @ws/simple-react-app build",
    "analyze": "yarn workspace @ws/simple-react-app analyze",
    "start": "yarn workspace @ws/simple-react-app start",
    "server": "yarn workspace @ws/simple-node-server start",
    
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "simple-node-server"
    ]
  },
  "packageManager": "yarn@3.1.1",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },

 "lint-staged": {
   "src/**/*.{js,jsx,ts,tsx,json,css,scss,md}": [
     "prettier --write"

      ]

    },
"pre-commit": "lint-staged",
  "dependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.1.4",
    "prettier": "^2.5.1"
  }
}
